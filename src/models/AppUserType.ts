export type AppUserType = {
    id?: number,
    userName: string,
    firstName: string,
    lastName: string,
    email: string,
    password?: string,
    roleId?: number,
    isActive?: boolean
};